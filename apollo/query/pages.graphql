query Pages {
  pages: allPageExamples {
    headerCtaText
    mainContent
    _meta {
      id
      name
      published
    }
  }
  tweets: allTweetExamples {
    message
    url
    hashtags
  }
}
